spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true

odsay:
  url: https://api.odsay.com/v1/api/searchPubTransPathT
  api-key: testApiKey

google:
  maps:
    api-key: testApiKey
