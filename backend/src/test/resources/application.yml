spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true

odsay:
  url: https://api.odsay.com/v1/api/searchPubTransPathT
  api-key: testApiKey

google:
  maps:
    api-key: testApiKey
